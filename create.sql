CREATE TABLE planets(id INTEGER PRIMARY KEY,
                      name TEXT,
                      color TEXT,
                      num_of_moons integer,
                      mass REAL
                  );
